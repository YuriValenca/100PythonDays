# Ao longo dos códigos anteriores, diversas funções foram utilizadas múltiplas vezes. Algumas executadas devido à importação de um módulo, outras que são do próprio Python. As funções são identificadas pelos parênteses, como print(), len(), int(), etc.

# É possível ainda criar próprias funções, seguindo o padrão def minhaFunção(argumentos) e dentro dela, de maneira IDENTADA, tem o bloco de código que ela realiza. Para invocar a função, basta chamá-la no código: minhaFunção(argumentos). Lembrando que uma função pode ter zero ou mais argumentos

def myFunction():  # Definição da função
    print("Oi!")
    print("Tchau!")

myFunction()  # Chamada da função

